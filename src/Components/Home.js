import { Typography, Box } from "@mui/material";

export const Home = () => {
  return (
    <Box sx={{ display: "flex", justifyContent: "center", marginTop: "16em" }}>
      <Typography variant="h1">Welcome to Fitness</Typography>;
    </Box>
  );
};
